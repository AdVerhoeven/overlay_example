# If you removed the bad one, this one will apply, but it will apply before the other overlays
# This means that your 1_20_3 should not reach this, because instead of this your_namespace:demo function
# The one from the overlay for format 26 will be called.
function your_namespace:macro {message: "16 and above, better practice"}